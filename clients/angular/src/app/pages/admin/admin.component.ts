import { Component } from '@angular/core';

@Component({
  selector: 'app-admin',
  standalone: true,
  template: `<h1>Admin</h1><p>Tenant settings, user management, roles, rules configuration, scheduled jobs.</p>`,
})
export class AdminComponent {}
