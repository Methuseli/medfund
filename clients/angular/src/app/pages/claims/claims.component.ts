import { Component } from '@angular/core';

@Component({
  selector: 'app-claims',
  standalone: true,
  template: `<h1>Claims</h1><p>Claims management portal — adjudication queue, claim details, pre-authorizations.</p>`,
})
export class ClaimsComponent {}
